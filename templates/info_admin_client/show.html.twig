{% extends 'base.html.twig' %}

{% block title %}Admin Client{% endblock %}

{% block body %}
    <h1>Info Client</h1>

    <table class="table">
        <tbody>
    
            <tr>
                <th>Name</th>
                <td>{{ info_admin_client.name }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ info_admin_client.poste }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ info_admin_client.numero }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ info_admin_client.email }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ info_admin_client.notes }}</td>
            </tr>
            <tr>
                <th>Entreprise</th>
                <td>{{ client.societyName }}</td>
            </tr>
            <tr>
                <th>Activité</th>
                <td>{{ client.typeActivite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('info_admin_client_index') }}">back to list</a>

    <a href="{{ path('info_admin_client_edit', {'id': info_admin_client.id}) }}">edit</a>

    {{ include('info_admin_client/_delete_form.html.twig') }}
{% endblock %}
