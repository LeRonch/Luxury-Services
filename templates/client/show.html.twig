{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Society_name</th>
                <td>{{ client.societyName }}</td>
            </tr>
            <tr>
                <th>type_Activite</th>
               <td> {{ client.typeActivite }}</td>         
            </tr>
        </tbody>
    </table>

    <a href="{{ path('info_admin_client_new', {'id': client.id}) }}">MODIFIER</a>
    <br>

    <a href="{{ path('client_index') }}">back to list</a>

    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
