{% extends 'base.html.twig' %}

{% block title %}Candidature{% endblock %}

{% block body %}
    <h1>Candidature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ candidature.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_candidature_index') }}">back to list</a>

    <a href="{{ path('admin_candidature_edit', {'id': candidature.id}) }}">edit</a>

    {{ include('admin_candidature/_delete_form.html.twig') }}
{% endblock %}
