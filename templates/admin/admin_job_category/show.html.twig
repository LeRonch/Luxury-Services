{% extends 'base.html.twig' %}

{% block title %}JobCategory{% endblock %}

{% block body %}
    <h1>JobCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_category.id }}</td>
            </tr>
            <tr>
                <th>Job_category</th>
                <td>{{ job_category.jobCategory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_job_category_index') }}">back to list</a>

    <a href="{{ path('admin_job_category_edit', {'id': job_category.id}) }}">edit</a>

    {{ include('admin_job_category/_delete_form.html.twig') }}
{% endblock %}
