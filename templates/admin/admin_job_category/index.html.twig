{% extends 'base_admin.html.twig' %}

{% block title %}JobCategory index{% endblock %}

{% block body %}
<div class="container mt-4">

    <h1>JobCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Job_category</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for job_category in job_categories %}
            <tr>
                <td>{{ job_category.id }}</td>
                <td>{{ job_category.jobCategory }}</td>
                <td>
                     <a href="{{ path('admin_job_category_edit', {id: job_category.id}) }}" class="btn btn-secondary">Editer</a>
                    <form method="post" action="{{ path('admin_job_category_delete', {'id': job_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline-block">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ job_category.id) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


    <div class="text-right">
            <a href="{{ path('admin_job_category_new') }}" class="btn btn-primary">Créer</a>
        </div>
    </div>
{% endblock %}
